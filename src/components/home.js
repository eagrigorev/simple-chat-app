export const Home = () => {
    return (
        <div>
            <h2>React Messaging App Prototype</h2>
            <p className="home">
                That chatting app was made with React and intended to highlight
                various tools of the framework. It has a built-in Google
                Firebase backend, Redux, Persist, password protection, third
                party API, login-register functionality, and so on.
            </p>
            <p>
                That project aimed to emulate a real conversation between users
                with a delayed message receiving mechanism, which was
                implemented using a middleware bot as a remote user.
            </p>
        </div>
    );
};
